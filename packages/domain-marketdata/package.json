{
    "name": "@zentrade/domain-marketdata",
    "version": "0.1.0",
    "private": true,
    "license": "MIT",
    "type": "module",
    "description": "Market-data domain logic: versioned, deterministic regime classification. Pure — no I/O.",
    "engines": {
        "node": ">=20"
    },
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run"
    },
    "dependencies": {
        "@zentrade/kernel": "*"
    },
    "devDependencies": {
        "typescript": "^5.7.3",
        "vitest": "^2.1.9"
    }
}
